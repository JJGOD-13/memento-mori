name: memento_mori
description: "A flutter app to help you visualise the finite time you ahve left and live life with purpose."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.3.3
  intl: ^0.18.0
  provider:
  url_launcher:
  dart_casing:
  test:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
